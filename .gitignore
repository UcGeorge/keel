# Binaries
/bin/
/dist/

# Local Keel state (created by `keel dev`) and compose data dir
keel-data/

# Terraform (state lives in GCS; runs use TF_DATA_DIR=/tmp)
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Node (used only to compile Tailwind CSS)
node_modules/

# OS noise
.DS_Store

# Env files
.env
.env.*
!.env.example

# Other
status*.txt
keys/
